{%- if article.image -%}
  <figure class="page-width">
    <img
      {% render 'srcset', image: article.image, width: 1000 %}
      alt="{{ article.image.alt | default: article.title }}"
      draggable="false" loading="eager"
    />
  </figure>
{%- endif -%}

<div class="page-width-sm py-16 px-4 paragraph">
  <h1 class="h1">{{ article.title }}</h1>

  <div class="richtext">
    {{ article.content
      | replace: '<h1>', '<h1 class="h1">'
      | replace: '<h2>', '<h2 class="h2">'
      | replace: '<h3>', '<h3 class="h3">'
      | replace: '<h4>', '<h4 class="h4">'
    }}
  </div>

  <hr>

  {%- if article.tags.size > 0 -%}
    <ul class="article-tag-list show-label">
      {%- for tag in article.tags -%}
        <li class="inline">
          {{- tag | link_to_tag: tag -}}
        </li>
      {%- endfor -%}
    </ul>
  {%- endif -%}

  <p><a href="{{ blog.url }}" class="link primary">< Back</a></p>
</div>

{% schema %}
{
  "name": "Article",
  "tag": "article",
  "class": "py-8 paragraph",
  "settings": [

  ]
}
{% endschema %}
