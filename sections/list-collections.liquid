<h1 class="text-5xl">All Collections</h1>

<ul>
  {% for collection in collections %}
    <li>
      <a href="{{ collection.url }}" class="link primary">
        {{ collection.title }}
      </a>
    </li>
  {% endfor %}
</ul>

{% schema %}
{
  "name": "All Collections",
  "tag": "article",
  "class": "page-width py-8 px-4",
  "settings": [

  ]
}
{% endschema %}
