<h1 class="h1">All Collections</h1>

<ul>
  {% for collection in collections %}
    <li>
      <a href="{{ collection.url }}" class="link accent-1">
        {{ collection.title }}
      </a>
    </li>
  {% endfor %}
</ul>

{% schema %}
{
  "name": "All Collections",
  "tag": "article",
  "class": "container padding",
  "settings": [

  ]
}
{% endschema %}
